module declix.dpkg

import ".../declix.pkl"

class Package extends declix.Resource {
    fixed type = "dpkg"
    fixed id = type + ":" + name

    name: String
    state: State
    content: declix.Content
}

typealias State = "installed" | "missing";